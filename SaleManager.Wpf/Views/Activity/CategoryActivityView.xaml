<UserControl x:Class="SaleManager.Wpf.Views.Activity.CategoryActivityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SaleManager.Wpf.Views.Activity"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Ellipse Grid.Column="0" Width="48" Height="48" Fill="{StaticResource BackgroundBrush}" Margin="0,0,12,0" VerticalAlignment="Top" />

        <StackPanel Grid.Column="1">
            <TextBlock x:Name="Title" FontSize="16" FontWeight="SemiBold" />
            <TextBlock x:Name="Name"/>
        </StackPanel>

    </Grid>
</UserControl>
